const navbarState = {
  data: {
    company: "My Logo",
    logo: "https://logo.clearbit.com/spotify.com?size=80&grayscale=true",
  },
}
const navbarReducer = (state = navbarState, action) => {
  return state
}

export default navbarReducer
